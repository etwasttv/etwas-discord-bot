name: Deploy to B590

on: [ workflow_dispatch ]

jobs:
  build:
    runs-on: self-hosted
    defaults:
      run:
        working-directory: ./bot
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20.11.1
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'
      - run: npm ci
      - run: |
          touch src/config.json
          echo '{"BOT_TOKEN": "${{ secrets.BOT_TOKEN }}","BOT_CLIENT_ID": "${{ secrets.BOT_CLIENT_ID }}","VOICEVOX_ENDPOINT": "${{ secrets.VOICEVOX_ENDPOINT }}","CNNAPI_ENDPOINT": "${{ secrets.CNNAPI_ENDPOINT }}"}' >> src/config.json
      - run: |
          touch .env
          echo ${{ secrets.DATABASE }} >> .env
      - name: Deploy Application Command
        run: npm run deploy-command
      - run: npx prisma migrate deploy
      - run: npx prisma generate
      - run: npm run build
      - run: pm2 startOrRestart ecosystem.config.js --env production --update-env
